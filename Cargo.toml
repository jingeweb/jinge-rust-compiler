[package]
name = "jinge-compiler"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
sha2 = "0.11.0"
base64ct = "1.8.3"
swc_core = { version = "79.0.0", features = ["ecma_plugin_transform"] }
swc_ecma_parser = "45.1.3"
swc_common = "26.0.0"
swc_ecma_codegen = "32.0.2"
swc_ecma_transforms_base = "49.0.1"
swc_ecma_transforms_typescript = "55.0.4"
swc_ecma_visit = "29.0.0"
lazy_static = "1.5.0"
hashbrown = "0.17.1"
neon = { version = "1.1.1", features = ["napi-6"] }

# .cargo/config defines few alias to build plugin.
# cargo build-wasi generates wasm-wasi32 binary
# cargo build-wasm32 generates wasm32-unknown-unknown binary.
